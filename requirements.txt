# Production runtime dependencies only
# (excludes streamlit, kagglehub, spacy, matplotlib, seaborn, openpyxl — not needed at inference time)
pandas>=2.0.0
scikit-learn>=1.3.0
joblib>=1.3.0
numpy>=1.24.0
fastapi>=0.104.0
uvicorn>=0.24.0
python-multipart>=0.0.6
pydantic>=2.0.0
